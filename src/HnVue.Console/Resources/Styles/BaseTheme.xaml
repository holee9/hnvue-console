<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:converters="clr-namespace:HnVue.Console.Converters">

    <!-- Converters -->
    <converters:StatusToBrushConverter x:Key="StatusToBrushConverter"/>
    <converters:StatusToStringConverter x:Key="StatusToStringConverter"/>
    <converters:WorklistStatusToBrushConverter x:Key="WorklistStatusToBrushConverter"/>
    <converters:WorklistStatusToStringConverter x:Key="WorklistStatusToStringConverter"/>
    <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
    <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    <converters:FocalSpotSizeToStringConverter x:Key="FocalSpotSizeToStringConverter"/>
    <converters:AecModeToBrushConverter x:Key="AecModeToBrushConverter"/>
    <converters:AecStatusToStringConverter x:Key="AecStatusToStringConverter"/>
    <converters:AecModeToDescriptionConverter x:Key="AecModeToDescriptionConverter"/>
    <converters:DoseAlertLevelToBrushConverter x:Key="DoseAlertLevelToBrushConverter"/>
    <converters:BoolToInverseConverter x:Key="BoolToInverseConverter"/>
    <!-- Image Review Converters -->
    <converters:ZoomTransformConverter x:Key="ZoomTransformConverter"/>
    <converters:OrientationTransformConverter x:Key="OrientationTransformConverter"/>
    <converters:OrientationToStringConverter x:Key="OrientationToStringConverter"/>
    <converters:PointsToPolylineConverter x:Key="PointsToPolylineConverter"/>
    <converters:CenterCanvasConverter x:Key="CenterCanvasConverter"/>
    <converters:OffsetCanvasConverter x:Key="OffsetCanvasConverter"/>
    <converters:MeasurementTypeToInstructionsConverter x:Key="MeasurementTypeToInstructionsConverter"/>
    <converters:RejectionReasonToStringConverter x:Key="RejectionReasonToStringConverter"/>
    <!-- System Status & Config Converters -->
    <converters:ComponentHealthToBrushConverter x:Key="HealthToBrushConverter"/>
    <converters:ComponentHealthToStringConverter x:Key="HealthToStringConverter"/>
    <converters:ComponentIdToNameConverter x:Key="ComponentIdToNameConverter"/>
    <converters:AlertsToStringConverter x:Key="AlertsToStringConverter"/>
    <converters:BoolToSuccessBrushConverter x:Key="BoolToSuccessBrushConverter"/>
    <converters:OutcomeToBrushConverter x:Key="OutcomeToBrushConverter"/>

    <!-- Button Styles -->
    <Style x:Key="PrimaryButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WhiteBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="{StaticResource MediumPadding}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource MediumCornerRadius}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                        VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{StaticResource DisabledTextBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource DisabledTextBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SecondaryButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource CardBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="{StaticResource MediumPadding}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource MediumCornerRadius}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                        VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource BackgroundBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="NavigationButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
        <Setter Property="Margin" Value="{StaticResource SmallLeftMargin}"/>
        <Setter Property="MinWidth" Value="100"/>
    </Style>

    <Style x:Key="WarningButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
        <Setter Property="Background" Value="{StaticResource ErrorBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ErrorBrush}"/>
    </Style>

    <!-- TextBox Styles -->
    <Style x:Key="BaseTextBoxStyle" TargetType="TextBox">
        <Setter Property="Padding" Value="{StaticResource SmallPadding}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Background" Value="{StaticResource CardBrush}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Style.Triggers>
            <Trigger Property="IsFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource FocusBorderBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ComboBox Styles -->
    <Style x:Key="BaseComboBoxStyle" TargetType="ComboBox">
        <Setter Property="Padding" Value="{StaticResource SmallPadding}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Background" Value="{StaticResource CardBrush}"/>
    </Style>

    <!-- CheckBox Styles -->
    <Style x:Key="BaseCheckBoxStyle" TargetType="CheckBox">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="{StaticResource SmallRightMargin}"/>
    </Style>

    <!-- DataGrid Styles -->
    <Style x:Key="BaseDataGridStyle" TargetType="DataGrid">
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="IsReadOnly" Value="True"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserDeleteRows" Value="False"/>
        <Setter Property="SelectionMode" Value="Single"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="AlternatingRowBackground" Value="{StaticResource BackgroundBrush}"/>
        <Setter Property="RowBackground" Value="{StaticResource CardBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
    </Style>

    <!-- Card Style -->
    <Style x:Key="CardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CardBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="{StaticResource MediumCornerRadius}"/>
        <Setter Property="Padding" Value="{StaticResource MediumPadding}"/>
        <Setter Property="Margin" Value="{StaticResource SmallMargin}"/>
    </Style>

    <!-- StatusBar Styles -->
    <Style x:Key="StatusTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource DefaultFontFamily}"/>
        <Setter Property="FontSize" Value="{StaticResource BodyFontSize}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
    </Style>

</ResourceDictionary>
