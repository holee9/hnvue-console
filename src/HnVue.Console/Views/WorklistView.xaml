<UserControl x:Class="HnVue.Console.Views.WorklistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Loaded="OnLoaded">

    <Border Style="{StaticResource CardStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Grid.Row="0"
                       Text="Modality Worklist"
                       Style="{StaticResource TitleTextStyle}"
                       Margin="{StaticResource MediumPadding}"/>

            <!-- Toolbar -->
            <Border Grid.Row="1"
                    Background="{StaticResource HeaderBrush}"
                    Padding="{StaticResource MediumPadding}"
                    Margin="{StaticResource MediumPadding}"
                    CornerRadius="{StaticResource MediumCornerRadius}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Patient Filter -->
                    <TextBlock Grid.Column="0">
                        <Run Text="Filter by Patient ID: " FontWeight="SemiBold"/>
                        <Run Text="(All patients shown when empty)" Style="{StaticResource CaptionTextStyle}"/>
                    </TextBlock>

                    <!-- Refresh Button -->
                    <Button Grid.Column="1"
                            Content="Refresh"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Command="{Binding RefreshCommand}"
                            MinWidth="100"
                            Margin="{StaticResource SmallLeftMargin}"/>

                    <!-- Loading Indicator -->
                    <StackPanel Grid.Column="0"
                                Grid.ColumnSpan="2"
                                Orientation="Horizontal"
                                HorizontalAlignment="Center"
                                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                                Margin="{StaticResource SmallTopMargin}">
                        <ProgressBar Width="200" Height="4" IsIndeterminate="True"/>
                        <TextBlock Text="Refreshing worklist..."
                                   Style="{StaticResource BodyTextStyle}"
                                   Margin="{StaticResource SmallLeftMargin}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Worklist DataGrid -->
            <Border Grid.Row="2"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="{StaticResource MediumCornerRadius}"
                    Margin="{StaticResource MediumPadding}"
                    Padding="{StaticResource SmallPadding}">
                <DataGrid ItemsSource="{Binding WorklistItems}"
                          Style="{StaticResource BaseDataGridStyle}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          GridLinesVisibility="All">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Patient ID"
                                          Binding="{Binding PatientId}"
                                          Width="120"/>
                        <DataGridTextColumn Header="Patient Name"
                                          Binding="{Binding PatientName}"
                                          Width="*"/>
                        <DataGridTextColumn Header="Accession #"
                                          Binding="{Binding AccessionNumber}"
                                          Width="100"/>
                        <DataGridTextColumn Header="Procedure"
                                          Binding="{Binding ScheduledProcedureStepDescription}"
                                          Width="200"/>
                        <DataGridTextColumn Header="Scheduled Date"
                                          Binding="{Binding ScheduledDateTime, StringFormat=yyyy-MM-dd HH:mm}"
                                          Width="130"/>
                        <DataGridTextColumn Header="Body Part"
                                          Binding="{Binding BodyPart}"
                                          Width="100"/>
                        <DataGridTextColumn Header="View"
                                          Binding="{Binding Projection}"
                                          Width="100"/>
                        <DataGridTemplateColumn Header="Status" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{Binding Status, Converter={StaticResource WorklistStatusToBrushConverter}}"
                                            CornerRadius="3"
                                            Padding="4,2">
                                        <TextBlock Text="{Binding Status, Converter={StaticResource WorklistStatusToStringConverter}}"
                                                   FontSize="10"
                                                   Foreground="{StaticResource WhiteBrush}"
                                                   FontWeight="SemiBold"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Action" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Select"
                                            Style="{StaticResource PrimaryButtonStyle}"
                                            Command="{Binding DataContext.SelectProcedureCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Padding="{StaticResource SmallPadding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- Status Bar -->
            <Border Grid.Row="3"
                    Background="{StaticResource BackgroundBrush}"
                    Padding="{StaticResource MediumPadding}"
                    Margin="{StaticResource MediumPadding}"
                    CornerRadius="{StaticResource MediumCornerRadius}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Item Count -->
                    <TextBlock Grid.Column="0">
                        <Run Text="Procedures: " FontWeight="SemiBold"/>
                        <Run Text="{Binding WorklistItems.Count, Mode=OneWay}"/>
                    </TextBlock>

                    <!-- Last Refreshed -->
                    <TextBlock Grid.Column="1">
                        <Run Text="Last Refreshed: " FontWeight="SemiBold"/>
                        <Run Text="{Binding LastRefreshedDisplay, Mode=OneWay}"/>
                    </TextBlock>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
