<UserControl x:Class="HnVue.Console.Views.ImageReviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:panels="clr-namespace:HnVue.Console.Views.Panels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             Loaded="OnLoaded">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0"
                Background="{StaticResource HeaderBrush}"
                Padding="{StaticResource MediumPadding}">
            <TextBlock Text="Image Review"
                       Style="{StaticResource TitleTextStyle}"
                       Foreground="{StaticResource WhiteBrush}"/>
        </Border>

        <!-- Main Image Viewer -->
        <panels:ImageViewerPanel Grid.Column="0" Grid.Row="1"
                                  Margin="{StaticResource MediumPadding}"/>

        <!-- Side Panel -->
        <ScrollViewer Grid.Column="1" Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      Margin="{StaticResource MediumPadding}">
            <StackPanel>
                <!-- Measurement Tools -->
                <panels:MeasurementToolPanel Margin="{StaticResource SmallBottomMargin}"/>

                <!-- QC Actions -->
                <panels:QCActionPanel Margin="{StaticResource SmallBottomMargin}"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
